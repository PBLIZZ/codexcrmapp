{
  // CRITICAL FIX: Point to the correct shared config package.
  "extends": "@codexcrm/config-typescript/base.json",
  "include": [
    "src"
  ],
  "exclude": [
    "dist",
    "node_modules"
  ],
  "compilerOptions": {
    // It is good practice to explicitly state that this is a composite project for 'references' to work.
    "composite": true
  }
}